# network-log-traffic-analyzer
Network log analyzer for firewall traffic. Parses logs, finds top talkers, detects blocked connections, and generates reports. Built with Python, works with CSV/JSONL/text formats.
